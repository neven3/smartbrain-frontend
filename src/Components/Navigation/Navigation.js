import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className='f3 link pointer dim black underline pa3'>Sign Out</p>
        </nav>
    );
};

export default Navigation;